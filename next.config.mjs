/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    formats: ["image/avif", "image/webp"],
  },
  // Renderizado estático por defecto (SSG). Las rutas que necesitan datos
  // cambiantes (tipos de interes, precios/m2) usan `export const revalidate`
  // (ISR) en su propio archivo, no aqui a nivel global.
  async headers() {
    return [
      {
        source: "/:path*",
        headers: [
          { key: "X-Content-Type-Options", value: "nosniff" },
          { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
        ],
      },
    ];
  },
};

export default nextConfig;
